--- a/certs/Makefile	2022-02-14 09:13:30.000000000 +1300
+++ b/certs/Makefile	2022-02-19 14:20:48.599936295 +1300
@@ -90,4 +90,4 @@
 hostprogs := extract-cert
 
 HOSTCFLAGS_extract-cert.o = $(shell pkg-config --cflags libcrypto 2> /dev/null)
-HOSTLDLIBS_extract-cert = $(shell pkg-config --libs libcrypto 2> /dev/null || echo -lcrypto)
+HOSTLDLIBS_extract-cert = $(shell pkg-config --libs libcrypto libpthread 2> /dev/null || echo -lcrypto -lpthread)
